variable n has_type Number is 80;

variable a has_type Number is 0; 
variable b has_type Number is 1; 

variable i has_type Number is 1;

loop_while (i LESS_EQUAL n) {
	variable next has_type Number is add(a  b);
	a is b;
	b is next;
	i is add(i 1);
	write_to_console a;
} 
